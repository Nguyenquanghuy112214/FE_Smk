const routes = {
  homepage: '/',
  school: '/school',
  register: '/register',
  login: '/login',
  vocabulary: '/vocabulary',
  grammar: '/grammar',
  learning: '/learning',
};

export default routes;
